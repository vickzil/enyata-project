export const baseUrl = (state) => {
  return state.baseUrl;
};

export const currentPage = (state) => {
  return state.currentPage;
};

export const mobileMenu = (state) => {
  return state.mobileMenu;
};

export const films = (state) => {
  return state.films;
};

export const starships = (state) => {
  return state.starships;
};

export const people = (state) => {
  return state.people;
};

export const species = (state) => {
  return state.species;
};

export const filmDetail = (state) => {
  return state.filmDetail;
};

export const starshipDetail = (state) => {
  return state.starshipDetail;
};

export const peopleDetail = (state) => {
  return state.peopleDetail;
};

export const speciesDetail = (state) => {
  return state.speciesDetail;
};
